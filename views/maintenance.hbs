<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{websiteTitle}}{{pageTitle}}</title>
    </head>
    <body>
        {{> header}}
        
        <p>{{> maintenanceMsg}}</p>

        {{> footer}}
    </body> 
</html>